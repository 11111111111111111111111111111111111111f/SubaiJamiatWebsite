<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>donate us</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Poppins', sans-serif;
        }

        .container {
            height: 100vh;
            width: 100%;
            display: flex;
            justify-content: center;
            align-items: center;
            background: #00d999;
        }

        form {

            background: white;
            width: 500px;
            border-radius: 5px;
            box-shadow: 2px 2px 5px rgba(0, 0, 0, 0.5);
            max-width: 100%;
            padding: 20px;

        }

        form label {

            display: block;
            word-break: break-all;
            margin-bottom: 2px;

        }

        form input,
        select {

            height: 40px;
            padding-inline: 10px;
            border-radius: 3px;
            border: 2px solid #00d999;
            width: 100%;
            outline: none;
            font-size: 16px;

        }

        form div {
            margin: 10px 0;
        }

        .heading {
            font-weight: 600;
            font-size: 35px;
            text-align: center;
        }

        button {
            background: #00d999;
            border: none;
            padding: 10px 20px;
            border-radius: 3px;
            color: white;
            font-size: 16px;
            cursor: pointer;
            transition: all 0.3s ease;

        }

        button:hover {
            background: #02b680;
        }

        .submit-container {
            text-align: center;
            margin-bottom: 0;

        }

        .error {
            color: crimson;
            font-size: 14px;
            text-align: right;
            display: block;
        }

        .success {
            color: #00d999;
        }

        .center {
            text-align: center;
        }

        h1 {
            font-size: 25px !important;
            text-align: center !important;
        }
    </style>

</head>

<body>

    <div class="container">
        <form>

            <h1 class="error">
                Payment cancelled or something went wrong
            </h1>
            <div class="center">
                <a href="/" class="success">Go back to home page</a>
            </div>
        </form>
    </div>

</body>

</html>